#!/usr/bin/env python
import sys
import os
import commands



if len(sys.argv) == 3:

	status, pwd = commands.getstatusoutput("m16hg "+sys.argv[1]+" "+sys.argv[2])
	print pwd


else:
	print "Not enough arguments."
